# Aswin Angras A — Portfolio

A personal portfolio website for Aswin Angras A, AI/ML Engineer.

## 📁 Folder Structure

```
aswina_portfolio/
├── index.html        ← Main HTML file (open this in browser)
├── css/
│   └── style.css     ← All styles
├── js/
│   └── main.js       ← Scroll animations & interactions
├── assets/           ← Add your profile photo here (optional)
└── README.md
```

## 🚀 How to Run

1. Open the folder in **VS Code**
2. Install the **Live Server** extension
3. Right-click `index.html` → **Open with Live Server**

Or simply double-click `index.html` to open in your browser.

## ✏️ Customization

- **Profile photo**: Add your image to `assets/` and update the avatar section in `index.html`
- **LinkedIn & GitHub**: Replace the `#` links in the Contact section with your real URLs
- **Projects**: Edit the project cards in `index.html` to add/update projects
- **Colors**: Change CSS variables at the top of `css/style.css`

## 🌐 Hosting (Free)

- **tiiny.site** — Drag & drop the zip file
- **GitHub Pages** — Push to a repo and enable Pages
- **Netlify** — Drag & drop the folder
